// import Content from './Content';
const Add = ({handleAdd}) =>{
    return(
        <main>
            <button onClick={handleAdd} className="add">Add</button>
        </main>
    )
};
export default Add;